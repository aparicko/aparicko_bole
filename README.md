## Initial Setup (Only Called the First Time)

```sh
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

# Usage (Called Every Time from the Console
```sh
source venv/bin/activate
python main.py
```

